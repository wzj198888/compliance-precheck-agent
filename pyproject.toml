[project]
name = "compliance-precheck-agent"
version = "0.1.0"
description = "A lightweight compliance precheck Agent prototype for SME contracts, procurement, reimbursements, and project approvals."
readme = "README.md"
requires-python = ">=3.10"
dependencies = []

[tool.setuptools.packages.find]
include = ["compliance_precheck*"]
